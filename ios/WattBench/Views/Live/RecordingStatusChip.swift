import SwiftUI

/// Red dot plus elapsed time shown in the Sessions tab while recording;
/// tapping it switches `AppRouter.tab` back to Live. Stub (WS-B fills it in);
/// the zero-argument init is frozen.
struct RecordingStatusChip: View {
    var body: some View {
        EmptyView()
    }
}
