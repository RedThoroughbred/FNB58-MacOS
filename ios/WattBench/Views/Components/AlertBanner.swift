import SwiftUI

/// Foreground alert banner placed by the Live tab via `safeAreaInset(edge: .top)`.
/// Stub (WS-E fills it in); the zero-argument init is frozen.
struct AlertBanner: View {
    var body: some View {
        EmptyView()
    }
}
